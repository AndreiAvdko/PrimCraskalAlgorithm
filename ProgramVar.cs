// Переменные программы
public enum GraphSettings : int
{
    // Верхний предел вершин для генерации графа
    minVertexCount  = 15,
    maxVertexCounter = 17,
    // Верхний предел количесва рёбер для генерации графа
    // Нижний предел значения рёбер должен быть больше, чем верхний у значения вершин
    minEdgeCounter = 25,
    maxEdgeCounter = 30,
    // Верхний предел значения веса для рёбер графа
    weightHighValue = 200

}
